;; Only compiles Rocq files.
(alias
 (name rocq-theories)
 (deps
  (alias_rec theories/all)))

;; Compile everything (including documentation).
(alias
 (name default)
 (deps
  ; (alias_rec doc)
  (alias rocq-theories)))
